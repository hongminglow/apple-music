Drop your own audio files into public/audio and update src/music/library.ts to point at them.

Out of the box, this repo generates a few synthetic WAV files via scripts/generate_synthetic_audio.py so playback works without any copyrighted assets.
